{
  "name": "mi-backend",
  "version": "1.0.0",
  "description": "este es mi primer backen con node",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env --watch src/index.js",
    "build": "esbuild src/index.js --bundle --platform=node --outfile=dist/index.cjs --minify",
    "start": "node --env-file=.env dist/index.cjs"
  },
  "keywords": [
    "backend",
    "rolling"
  ],
  "author": "Alexis Gunsett",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  }
}
